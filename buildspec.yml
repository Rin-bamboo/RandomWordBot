version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - echo "Installing dependencies..."
      - pip install -r requirements.txt

  pre_build:
    commands:
      - echo "Running pre-build steps..."
      # ここに前処理のコマンドやスクリプトを追加

  build:
    commands:
      - echo "Building the project..."
      # ここにビルドのコマンドやスクリプトを追加

  post_build:
    commands:
      - echo "Running post-build steps..."
      # ここに後処理のコマンドやスクリプトを追加

artifacts:
  files:
    - '**/*'
  name: my-build-output